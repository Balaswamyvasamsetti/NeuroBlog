{
  "name": "neuroblog",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm run build",
    "start": "node server/server.js",
    "install": "npm install && cd client && npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}